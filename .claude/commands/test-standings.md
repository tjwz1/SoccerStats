Run competition standings tests: table load, zone indicators (UCL/UEL/ECL/relegation/playoff), zone legend, form pips, season selector, group selector, bracket view, team navigation, and error states.

```bash
cd e2e && python -m pytest -m standings -v
```
