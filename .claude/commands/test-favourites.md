Run favourites workflow tests: starring/unstarring teams, sidebar chips appearing and linking to teams, chip removal (× button and standings unstar), multiple favourites, localStorage persistence across reload and navigation, and the My Teams calendar filter.

```bash
cd e2e && python -m pytest -m favourites -v
```
