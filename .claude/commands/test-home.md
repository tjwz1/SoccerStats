Run home page tests: fixture calendar, month navigation, day selection, My Teams filter, theme toggle, navigation breadcrumbs, and fixture card interactions.

```bash
cd e2e && python -m pytest -m "calendar or navigation or theme" -v
```
