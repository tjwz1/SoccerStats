Run player career stats page tests: identity fields, career totals, this-season stats, season-by-season table, honours, empty states, photo/monogram fallback, and navigation.

```bash
cd e2e && python -m pytest -m player -v
```
